le ret_i32_1(number:ref i32)->i32{
    while(number>0){ ## while loop and if else branch
        if (number==5){
            ret number;
        }el{
            number=number-1;
        }
    }
    ret number;
}
